<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="anreal_splash_background">#050505</color>

    <!-- Shared attributes for every API level. -->
    <style name="Theme.Anreal.Base" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/anreal_splash_background</item>
    </style>

    <!-- API 24-26 base; windowLightNavigationBar needs 27 and the contrast
         attributes need 29, so those live in values-v27 and values-v29. -->
    <style name="Theme.Anreal" parent="Theme.Anreal.Base" />

    <style name="Theme.Anreal.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/anreal_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Anreal</item>
    </style>
</resources>
